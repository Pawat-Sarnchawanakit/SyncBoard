# 64 byte hex
AUTH_TOKEN_SECRET=
# Host you want to start server on
HOST=
# Database type: sqlite or postgres (default: postgres)
DB_TYPE=postgres
# Database configuration (only used when DB_TYPE=postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=syncboard